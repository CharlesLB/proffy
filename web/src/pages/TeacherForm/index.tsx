import React from "react";

import PageHeader from "../../components/PageHeader";

const TeacherForm: React.FC = () => {
    return (
        <div id="page-teacher-form" className="container">
            <PageHeader title="Será feito amanhã." />
        </div>
    );
};

export default TeacherForm;
